services:
  fokus:
    image: ghcr.io/klppl/fokus:latest
    container_name: fokus
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DATABASE_URL: file:./data/fokus.db
    volumes:
      - fokus_data:/app/data

volumes:
  fokus_data:
